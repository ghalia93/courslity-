// Defines shared TypeScript types for filters data.
export interface Filters {
  university: string;
  department: string;
  level: string;
  language: string;
   year?: string;
  semester?: string;
}
